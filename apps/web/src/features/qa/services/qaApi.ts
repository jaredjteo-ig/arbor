/* ── QA Feature — API Service Re-export ──────────────────── */
/* Primary implementation lives in services/api/qa.ts to      */
/* follow the existing API service pattern.                    */

export { qaApi } from "@/services/api/qa";
