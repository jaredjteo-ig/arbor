// Employee attendance route — reuses the shared attendance page which already
// renders differently based on the user's role (employee sees "My Attendance" view).
export { default } from "../attendance/page";
