# Session Notes — 2026-04-05

### Read These Files First (in order)

1. `src/hr_advisory/delegate/arbor_loop.py` — Delegate engine: run_delegate_sync() + create_delegate() + system prompt wiring
2. `src/hr_advisory/delegate/system_prompt.py` — Mandatory tool use prompt (208 tools, MUST call tools before answering)
3. `src/hr_advisory/services/dataflow_crud.py` — Shared CRUD module: all single-record ops use db.express_sync (588 calls)
4. `src/hr_advisory/models/database.py` — Pool config: env-driven DATAFLOW_MAX_CONNECTIONS, 5s timeout, sqlite fallback
5. `src/hr_advisory/api/routers/advisory.py:363-393` — /advisory/query now uses Delegate (was AdvisoryEngine)
6. `src/hr_advisory/workflows/guardrails.py:483-540` — Scope classifier uses Delegate (was raw openai.OpenAI)
7. `.claude/VERSION` — COC v2.5.0, SDK build_version 2.5.1

## Accomplished

- **COC synced v1.2.0 → v2.5.0** (10 releases, 468 files). 17 stale artifacts removed. CC audit converged.
- **144 WorkflowBuilder CRUD → db.express_sync** via shared dataflow_crud.py. 29 files migrated. 12 WFB remain (all multi-step).
- **Advisory engine → Kaizen Delegate** for /advisory/query. 208 tools, autonomous multi-tool orchestration verified.
- **Guardrails + QA tools → Delegate**. Raw OpenAI eliminated from production path (1 remains: embeddings API).
- **All hardcoded model defaults removed** (11→0). All hardcoded DB credentials removed (5→0).
- **Security fixes**: NaN confidence guard, async ThreadPoolExecutor fallback, API key repr=False, DB URL fail-fast.
- **Performance**: search caching (5-min TTL), compliance batch queries, advisory 60s timeout, pool env config.
- **Tool call tracking fixed**: ToolCallStart.name (not .tool_name). JSON leakage stripped from response text.
- **Quality audit**: 5 queries tested — CPF calc, leave, multi-tool quota+CPF, termination, maternity. All correct.
- **PR #12 merged**, v0.3.0 released, v0.3.2 deployed to GCE arbor-prod. Production healthy.

## Outstanding

- [ ] `kb/embeddings.py:79` — raw `openai.OpenAI` for embeddings API (no Delegate equivalent; acceptable)
- [ ] `advisory_engine.py` — deprecated but still importable. Remove entirely when all consumers migrated
- [ ] kailash-rs issues #186-190 open (5 issues on esperie-enterprise/kailash-rs)
- [ ] kailash-py #283 (Nexus handler validation) — partially fixed, stays open
- [ ] PACT governance integration (M60) — blocked on kailash-pact GovernanceContext
- [ ] Production .env persistence — credentials in /opt/arbor/.db-password etc. Deploy must source them.

### Oversight — Verify Before Starting

- [ ] `curl -sf https://arbor.terrene.foundation/api/health` — production healthy (was: 200 OK)
- [ ] `uv pip show kailash | grep Version` — should be 2.5.1
- [ ] `git log --oneline -3` — latest is fix(delegate) on main
- [ ] `grep -r 'WorkflowBuilder()' src/hr_advisory/ --include='*.py' | grep -v __pycache__ | wc -l` — should be 12

### Oversight — Verify After Completing

- [ ] `uv run pytest tests/unit/ -x -q -p no:warnings` — 1,155+ tests pass
- [ ] `grep -r 'dataflow_crud\.' src/hr_advisory/ --include='*.py' | grep -v __pycache__ | wc -l` — should be 588+
- [ ] No hardcoded models: `grep -rn '"gpt-' src/hr_advisory/ --include='*.py' | grep -v llm_budget | grep -v '#\|"""' | wc -l` — should be 0

## Blockers

- **PACT GovernanceContext** — requires kailash-pact integration (M60, not started)
- **Embeddings API** — no Kaizen Delegate equivalent for `client.embeddings.create()`; raw OpenAI is correct here
